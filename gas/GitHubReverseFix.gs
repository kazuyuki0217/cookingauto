/*
 * 料理アフィリエイト自動化
 * GitHub → GAS 反映補助
 *
 * 目的:
 * GitHub上のgas/配下を取得し、Apps Scriptプロジェクトへ反映するための
 * 安全な入口を用意する。
 *
 * 注意:
 * このファイル単体ではGASプロジェクトを書き換えない。
 * Apps Script APIの更新処理は、既存のGAS側認証・権限設定を確認したうえで実装する。
 */

function GitHubReverseFix_説明() {
  return 'GitHub → GAS反映機能の導入準備ファイルです。';
}
